import { SimpleDemo } from "./components/simple-demo";

/**
 * Enhanced Quiz Game - JSON-driven Colyseus demo with MCQ and True/False questions
 * Features: Multiple question types, real-time scoring, feedback system
 * Uses colyseus-hooks for clean React integration
 */
export function App() {
  return <SimpleDemo />;
}
